package maxigregrze.cobblesafari.block.misc;

import maxigregrze.cobblesafari.init.ModBlockEntities;
import net.minecraft.core.BlockPos;
import net.minecraft.world.level.block.state.BlockState;

public class UnionRoomGlobeUpperBlockEntity extends UnionRoomDecorBlockEntity {

    public UnionRoomGlobeUpperBlockEntity(BlockPos pos, BlockState state) {
        super(ModBlockEntities.UNION_ROOM_GLOBE_UPPER, pos, state);
    }
}
